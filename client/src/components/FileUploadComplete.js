import React, { Fragment } from 'react';

const FileUploadComplete = () => {

  // TODO: 
  // 1. Add fullscreen div and buttons to hide view

  return (
    <Fragment>
      
    </Fragment>
  );
};

export default FileUploadComplete;